var documenterSearchIndex = {"docs":
[{"category":"section","location":"objects/#Object-operations","page":"Object operations","text":"CloudStore accepts three forms for most object operations:\n\na store handle and object key;\na CloudStore.Object;\nan S3 or Azure object URL.\n\nThe examples below use the generic CloudStore interface. The CloudStore.S3 and CloudStore.Blobs namespaces provide matching methods.","title":"Object operations"},{"category":"section","location":"objects/#Upload","page":"Object operations","text":"Upload bytes, text, an open IO, or a file path:\n\nobj = CloudStore.put(bucket, \"reports/today.csv\", codeunits(\"a,b\\n1,2\\n\"))\nobj = CloudStore.put(bucket, \"reports/today.csv\", IOBuffer(data))\nobj = CloudStore.put(bucket, \"reports/today.csv\", \"/tmp/today.csv\")\n\nThe return value is an Object. It contains the store, key, byte size, and ETag.\n\nCloudStore starts a multipart upload above multipartThreshold. Control the transfer with partSize and batchSize:\n\nobj = CloudStore.put(\n    bucket,\n    \"large.bin\",\n    data;\n    multipartThreshold=8 * 1024^2,\n    partSize=8 * 1024^2,\n    batchSize=8,\n)\n\nSet allowMultipart=false to force one request. Set compress=true to gzip the uploaded bytes. CloudStore does not add .gz to the key.","title":"Upload"},{"category":"section","location":"objects/#Download","page":"Object operations","text":"Download into a new byte vector:\n\ndata = CloudStore.get(bucket, \"reports/today.csv\")\n\nDownload into an existing destination:\n\nbuffer = Vector{UInt8}(undef, expected_size)\nCloudStore.get(bucket, \"large.bin\", buffer)\n\nCloudStore.get(bucket, \"large.bin\", \"/tmp/large.bin\")\n\nopen(\"/tmp/large.bin\", \"w\") do io\n    CloudStore.get(bucket, \"large.bin\", io)\nend\n\nMultipart downloads first read object metadata. Use objectMaxSize when you know an upper bound and want to avoid that request for a small object. Set decompress=true to gunzip the downloaded bytes.","title":"Download"},{"category":"section","location":"objects/#Metadata-and-listing","page":"Object operations","text":"Read response headers without downloading the object:\n\nheaders = CloudStore.head(bucket, \"reports/today.csv\")\ncontent_length = parse(Int, headers[\"Content-Length\"])\n\nList every object, or limit the result to a prefix:\n\nobjects = CloudStore.list(bucket)\nreports = CloudStore.list(bucket; prefix=\"reports/\")\n\nCloudStore follows provider pagination until it has the complete result. maxKeys controls the page size. It does not limit the total returned object count.","title":"Metadata and listing"},{"category":"section","location":"objects/#Use-an-Object","page":"Object operations","text":"An upload or listing returns CloudStore.Object values. You can use an object as the source for later operations:\n\ndata = CloudStore.get(obj)\nheaders = CloudStore.head(obj)\nCloudStore.delete(obj)\n\nConstructing an object by key sends a metadata request:\n\nobj = CloudStore.Object(bucket, \"reports/today.csv\")","title":"Use an Object"},{"category":"section","location":"objects/#Delete","page":"Object operations","text":"Delete one object:\n\nCloudStore.delete(bucket, \"reports/today.csv\")\n\nCloudStore does not create or delete S3 buckets or Azure containers. Use the provider control plane for store lifecycle operations.","title":"Delete"},{"category":"section","location":"objects/#Pass-request-options","page":"Object operations","text":"Extra keyword arguments pass to the signed CloudBase HTTP request. This supports options such as timeouts, retries, and explicit credentials. Treat provider-specific headers and query parameters with care because they can change request signing.","title":"Pass request options"},{"category":"section","location":"streaming/#Streaming-transfers","page":"Streaming transfers","text":"Use a stream when the complete object should not be in memory at one time.","title":"Streaming transfers"},{"category":"section","location":"streaming/#Prefetched-downloads","page":"Streaming transfers","text":"CloudStore.PrefetchedDownloadStream reads ranges in parallel into two buffers. Construct it from an Object:\n\nobj = CloudStore.Object(bucket, \"large.csv\")\nio = CloudStore.PrefetchedDownloadStream(obj)\ntry\n    while !eof(io)\n        row = readline(io)\n        # Process the row.\n    end\nfinally\n    close(io)\nend\n\nTune the memory and request sizes when needed:\n\nio = CloudStore.PrefetchedDownloadStream(\n    obj,\n    16 * 1024^2;\n    prefetch_multipart_size=2 * 1024^2,\n)\n\nThe first positional size is the size of each in-memory prefetch buffer. The keyword value is the maximum size of each range request. The stream is read-only and is not thread-safe.","title":"Prefetched downloads"},{"category":"section","location":"streaming/#Multipart-uploads","page":"Streaming transfers","text":"CloudStore.MultipartUploadStream sends each written byte vector as one part. The do-block form waits for all parts and completes the upload:\n\nCloudStore.MultipartUploadStream(bucket, \"generated.bin\") do io\n    for chunk in chunks\n        write(io, chunk)\n    end\nend\n\nEach chunk must be a Vector{UInt8}. Except for the final part, Amazon S3 requires parts to meet its minimum part size. Use CloudStore.put for small objects.\n\nUse the manual form only when you need direct lifecycle control:\n\nio = CloudStore.MultipartUploadStream(bucket, \"generated.bin\")\nwrite(io, first_chunk)\nwrite(io, second_chunk)\nwait(io)\nclose(io)\n\nKeep chunks in object order. concurrent_writes_to_channel limits the number of uploads in flight and applies backpressure to write.","title":"Multipart uploads"},{"category":"section","location":"reference/#API-reference","page":"API Reference","text":"CloudStore uses a small namespace-based API. The main operations are:\n\nCloudStore.list(store; kwargs...)\nCloudStore.get(store, key[, destination]; kwargs...)\nCloudStore.head(store, key; kwargs...)\nCloudStore.put(store, key, source; kwargs...)\nCloudStore.delete(store, key; kwargs...)\n\nThe same operation names are available as CloudStore.S3.list, CloudStore.S3.get, and so on, and as CloudStore.Blobs.list, CloudStore.Blobs.get, and so on.","title":"API reference"},{"category":"section","location":"reference/#Object","page":"API Reference","text":"","title":"Object"},{"category":"section","location":"reference/#Transfer-streams","page":"API Reference","text":"","title":"Transfer streams"},{"category":"type","location":"reference/#CloudStore.API.Object","page":"API Reference","text":"CloudStore.Object(store, key; credentials=nothing, kwargs...)\n\nA remote object in an S3 bucket or Azure Blob container.\n\nConstructing an Object from a store and key sends a metadata request. Upload and list operations also return Object values. The fields include store, credentials, key, size, eTag, and provider-specific properties.\n\n\n\n\n\n","title":"CloudStore.API.Object"},{"category":"type","location":"reference/#CloudStore.API.PrefetchedDownloadStream","page":"API Reference","text":"PrefetchedDownloadStream{T <: Object} <: IO\nPrefetchedDownloadStream(args...; kwargs...) -> PrefetchedDownloadStream{T <: Object}\n\nA buffered, read-only, in-memory IO stream that fetches chunks from remote cloud Object.\n\nData is downloaded to two internal buffers. Once you start reading from the first buffer, a secondary buffer will begin to be \"prefetched\" by multiple background tasks. Once you read past the first buffer, the two buffers are switched and new round of prefetching begins.\n\nTo control memory usage and speed, the user can change two parameters when constructing the stream: prefetch_multipart_size and prefetch_size. prefetch_multipart_size is the max size of any individual GET request in bytes (default 8.000 MiB), prefetch_size is the size of a buffer that stores the fetched bytes and which is iterated when we consume/read the IO (default 32.000 MiB).\n\nThe number of spawned tasks is also governed by these two parameters, with approximately prefetch_size / prefetch_multipart_size tasks spawned for performing the GET requests (defaults to 4 if those fields aren't specified) + 1 task is spawned to coordinate the prefetching process. Number of spawned tasks is upper-bounded by the size of the input and the number of threads available (see the internal _ndownload_tasks helper function).\n\nReading from this stream is not thread-safe.\n\nArguments\n\nstore::AbstractStore: The S3 Bucket / Azure Container object\nkey::String: S3 key / Azure blob resource name\nprefetch_size::Int=DEFAULT_PREFETCH_SIZE: The size of each of the two internal prefetch   buffers in bytes\n\nKeywords\n\ncredentials::Union{CloudCredentials, Nothing}=nothing: Credentials object used in HTTP   requests\nprefetch_multipart_size::Int=DEFAULT_PREFETCH_MULTIPART_SIZE: The size of each individual  GET request in bytes\nkwargs...: HTTP keyword arguments are forwarded to underlying HTTP requests,\n\nExamples\n\n# Get an IO stream for a remote CSV file `test.csv` living in your S3 bucket\nio = PrefetchedDownloadStream(my_bucket, \"test.csv\"; credentials)\n\n# Integrates with TranscodingStreams; HTTP keyword arguments are forwarded to underlying HTTP requests\nusing CodecZlib\nio = GzipDecompressorStream(\n    PrefetchedDownloadStream(my_bucket, \"test.csv.gz\"; credentials, retries=5)\n)\n\n# Up to 8 concurrent download tasks, each fetching 2MiB into 16MiB prefetch buffer.\nio = PrefetchedDownloadStream(\n    my_bucket, \"test.csv\", 16*1024*1024; credentials, prefetch_multipart_size=2*1024*1024)\n)\n\n\n\n\n\n","title":"CloudStore.API.PrefetchedDownloadStream"},{"category":"type","location":"reference/#CloudStore.API.MultipartUploadStream","page":"API Reference","text":"This is an experimental API.\n\nMultipartUploadStream <: IO\nMultipartUploadStream(args...; kwargs...) -> MultipartUploadStream\n\nAn in-memory IO stream that uploads chunks to a URL in blob storage.\n\nFor every data chunk we call write(io, data;) to write it to a channel. We spawn one task per chunk to read data from this channel and uploads it as a distinct part to blob storage to the same remote object. We expect the chunks to be written in order. For cases where there is no need to upload data in parts or the data size is too small, put can be used instead.\n\nArguments\n\nstore::AbstractStore: The S3 Bucket / Azure Container object\nkey::String: S3 key / Azure blob resource name\n\nKeywords\n\ncredentials::Union{CloudCredentials, Nothing}=nothing: Credentials object used in HTTP   requests\nconcurrent_writes_to_channel::Int=(4 * Threads.nthreads()): represents the max number of   chunks in flight. Defaults to 4 times the number of threads. We use this value to   initialize a semaphore to perform throttling in case the writing to the channel is much   faster to uploading to blob storage, i.e. write will block as a result of this limit   being reached.\nkwargs...: HTTP keyword arguments are forwarded to underlying HTTP requests,\n\nExamples\n\n# Get an IO stream for a remote CSV file `test.csv` living in your S3 bucket\nio = MultipartUploadStream(bucket, \"test.csv\"; credentials)\n\n# Write a chunk of data (Vector{UInt8}) to the stream\nwrite(io, data;)\n\n# Wait for all chunks to be uploaded\nwait(io)\n\n# Close the stream\nclose(io; credentials)\n\n# Alternative syntax that encapsulates all these steps\nMultipartUploadStream(bucket, \"test.csv\"; credentials) do io\n    write(io, data;)\nend\n\nNote on upload size\n\nSome cloud storage providers might have a lower limit on the size of the uploaded object.\nFor example it seems that S3 requires at minimum an upload of 5MB:\nhttps://github.com/minio/minio/issues/11076.\nWe haven't found a similar setting for Azure.\nFor such cases where the size of the data is too small, one can use `put`\n\n\n\n\n\n","title":"CloudStore.API.MultipartUploadStream"},{"category":"function","location":"reference/#CloudStore.API.abort","page":"API Reference","text":"CloudStore.abort(io::MultipartUploadStream; kwargs...)\n\nWait for active part requests, then abort the multipart upload. S3 removes the uploaded parts immediately. Azure has no matching abort request and removes uncommitted blocks after its service retention period.\n\n\n\n\n\n","title":"CloudStore.API.abort"},{"category":"section","location":"#CloudStore.jl","page":"Home","text":"CloudStore.jl provides one object-storage interface for Amazon S3 and Azure Blob Storage. It supports object listing, upload, download, metadata, deletion, parallel transfers, and streaming transfers.\n\nCloudStore uses CloudBase.jl for credentials and signed HTTP requests. It does not create buckets, containers, or other cloud resources.","title":"CloudStore.jl"},{"category":"section","location":"#Installation","page":"Home","text":"Install CloudStore from the General registry:\n\nusing Pkg\nPkg.add(\"CloudStore\")\n\nLoad the package and the provider namespace that you need:\n\nusing CloudStore\n\nconst S3 = CloudStore.S3\nconst Blobs = CloudStore.Blobs\n\nCloudStore keeps its public names inside the package namespace. It does not add common names such as get, put, or delete to your session.","title":"Installation"},{"category":"section","location":"#Create-a-store","page":"Home","text":"Create an S3 bucket handle with its name and region:\n\nbucket = S3.Bucket(\"my-bucket\", \"us-west-2\")\n\nCreate an Azure container handle with its container name and storage account:\n\ncontainer = Blobs.Container(\"my-container\", \"mystorageaccount\")\n\nThese constructors do not send a network request. They only create a handle for later operations.","title":"Create a store"},{"category":"section","location":"#Credentials","page":"Home","text":"CloudStore accepts credentials as a keyword argument. CloudBase can also load credentials from the standard environment, configuration files, and cloud-host metadata services.\n\ndata = CloudStore.get(bucket, \"reports/today.csv\"; credentials)\n\nDo not put long-lived secrets in source code. Use your cloud provider's standard credential chain when possible.","title":"Credentials"},{"category":"section","location":"#Choose-an-interface","page":"Home","text":"The generic interface dispatches from the store type:\n\nCloudStore.put(bucket, \"hello.txt\", codeunits(\"hello\"))\nCloudStore.get(bucket, \"hello.txt\")\n\nThe provider namespaces offer the same operations:\n\nS3.put(bucket, \"hello.txt\", codeunits(\"hello\"))\nBlobs.put(container, \"hello.txt\", codeunits(\"hello\"))\n\nYou can also use an S3 or Azure object URL. Provide region for an S3 URL when the URL does not contain it.\n\nCloudStore.get(\"s3://my-bucket/reports/today.csv\"; region=\"us-west-2\")\nCloudStore.get(\"https://mystorageaccount.blob.core.windows.net/my-container/data.csv\")\n\nSee Object operations for the complete transfer workflow.","title":"Choose an interface"}]
}
